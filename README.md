# Dynamic QR Code Generator

เว็บสร้าง QR Code ที่สามารถแก้ไขปลายทางได้ภายหลัง (Dynamic QR)  
มีระบบนับจำนวนการสแกน เปลี่ยนสี ใส่โลโก้ และดาวน์โหลดได้

## 🔧 วิธีใช้งาน
1. สร้าง Firebase Firestore (collection: `qr_links`)
2. ใส่ค่า `firebaseConfig` ใน `src/firebase.js`
3. รันเว็บด้วยคำสั่ง:
   ```bash
   npm install
   npm run start
